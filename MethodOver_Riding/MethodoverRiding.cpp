/*
  Function overloading -: if function name same but argument are different means - overloading
  but if you define function in same name and different parameter which is in different- different
  class that time it is not overloading 
  that is called method hiding
  means function overloading is work only within a class or within a function;

*/
/*
   method over riding--same function in both class but access that fuction who crate obj for that class
   example
   if you define car class and sport car class and you define set_gear() in both class 
   and you want when you create car class obj that time car set_gear() bind and when 
   you want create obj for sport car class that time you want bind sport gear functionality
   that time use method over riding
*/